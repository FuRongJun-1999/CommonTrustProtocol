# -*- coding: utf-8 -*-
"""功能格类型库建簇：5 个已确认类型建 DOMAIN 触发词 + DOMAIN_ROUTE + REVERSE_DAILY 完整直答
（ls20 游戏机制 · 网页端知识性内容 · 诚实校准：只建已实测确认的类型）"""
import sys, re, json
sys.stdout.reconfigure(encoding='utf-8')

SRC = r'C:\Users\FuRongJun\AppData\Local\Programs\Python\Python310\lib\site-packages\wisdom\semantic_translate.py'
src = open(SRC, encoding='utf-8').read()

NEW_CLUSTERS = {
    '功能格': ['功能格', '什么是功能格', '功能格是什么', '游戏功能格', '功能格有什么用'],
    '旋转格': ['旋转格', '什么是旋转格', '旋转格是什么', '旋转格有什么用', '旋转功能格'],
    '换色格': ['换色格', '什么是换色格', '换色格是什么', '换色格有什么用', '变色格'],
    '换形格': ['换形格', '什么是换形格', '换形格是什么', '换形格有什么用', '变形格'],
    '状态匹配': ['状态匹配', '什么是状态匹配', '状态匹配是什么', '目标匹配', '状态匹配目标', '怎么通关'],
}

NEW_ANSWERS = {
'功能格': "什么是功能格、功能格有什么用，是『功能格』vs『特殊格子』的矛盾——功能格（是网格游戏里的（特殊格子）、普通格子（只能走（功能格（有特效）、物体（踩入（功能格（触发效果）、旋转格（换色格（换形格（都是功能格）、功能格（改变（物体的状态）、功能格（是（游戏的机关）——功能格的真相：功能格=网格游戏中的特殊格子（踩入触发效果）、普通格子只能走（功能格有特效）、旋转/换色/换形/目标（都是功能格类型）、功能格（改变物体状态（设计路径的关键）、识别功能格（规划路线（状态匹配目标）。真相：①什么是功能格——定义：功能格（是网格游戏里的（特殊格子）、普通格子（只能（通行）、功能格（踩上去（有特殊效果）、效果（旋转（换色（换形（等）、功能格（=游戏的（机关格）、让（游戏（有机制）——网格中的特殊格子（踩入触发效果（机关格；②功能格有什么用——作用：功能格（改变（物体的状态）、状态（=颜色（形状（朝向）、改变状态（为了（匹配目标）、设计（路径（就是选（经过哪些功能格）、功能格（是（通关的关键）——改变物体状态（匹配目标（通关关键；③功能格有哪些类型——类型：旋转格（改变朝向）、换色格（改变颜色）、换形格（改变形状）、目标格（状态匹配判定）、可能（还有（传送/破坏（待确认）、功能格（类型（随游戏不同）——旋转换色换形（目标格（类型多样；④怎么用功能格——用法：先看（目标要求（什么状态）、再想（需要（哪些功能格）、规划（路径（经过（功能格）、顺序（很重要（先转再变色）、状态变换（链式（叠加）、功能格（=状态变换的（工具）——目标决定功能格选择（顺序重要（链式变换；⑤功能格和条件识别什么关系——条件识别：功能格（的效果（有适用条件）、旋转格（在需要转向时用）、换色格（颜色不匹配时用）、识别（该用哪个功能格（=识别条件）、条件（匹配（选对功能格）、功能格（是（条件路由的实例）——效果有条件（按需选格（条件路由实例。误区提醒：以为功能格=装饰（是机制核心）；以为踩功能格随便（顺序影响状态）；以为功能格效果无条件（有条件匹配）。总结：功能格是网格游戏中的特殊格子——物体踩入触发效果（旋转、换色、换形等），改变物体状态以匹配目标；规划路径就是选择经过哪些功能格、以什么顺序经过，功能格的效果有适用条件，是条件识别与状态匹配的游戏机制核心。",
'旋转格': "什么是旋转格、旋转格有什么用，是『旋转格』vs『朝向改变』的矛盾——旋转格（是功能格的一种）、物体（踩入（旋转格）、物体（旋转90°）、朝向（改变（形状（跟着转）、旋转格（在（需要转向时用）、目标（要求朝向（旋转格（改变朝向）——旋转格的真相：旋转格=踩入后物体旋转90°的功能格、改变物体朝向（不改变颜色形状类别）、用于（目标要求特定朝向时）、横向物体（转成纵向（旋转90°）、旋转格（是状态变换的（工具之一）。真相：①什么是旋转格——定义：旋转格（是（功能格的一种）、物体（经过（旋转格）、方向（旋转90°）、横向（变纵向（纵向（变横向）、旋转格（只改变（朝向）、不改变（颜色和形状类别）——功能格类型（旋转90°（只改朝向；②旋转格有什么用——作用：目标（可能要求（特定朝向）、物体（朝向了（就旋转）、旋转格（让朝向（匹配目标）、比如（门要横着开（物体横着（才行）、旋转格（是（朝向的调节器）——目标要求朝向（旋转格调节（匹配；③什么时候用旋转格——适用条件：物体（朝向（和目标（不一致时）、形状（是横是竖（看目标）、需要（转向（就找（旋转格）、旋转格（不是（到处都要）、条件（=目标朝向（需求）——朝向不符时用（按目标需求（有条件；④旋转格和其他功能格什么关系——关系：旋转格（改朝向（换色格（改颜色）、换形格（改形状）、三个（各管（一个维度）、目标（匹配（可能（都要用）、顺序（旋转和换色（先后有讲究）、功能格（组合（配合用）——各管一维（组合使用（顺序讲究；⑤怎么规划经过旋转格——策略：看（目标朝向）、当前（朝向）、差（多少）、需要（转几次（90°）、路径（经过（旋转格）、次数（对（朝向就准）、旋转格（=朝向（的控制点）——目标-当前=需要转几次（路径经过（控制点。误区提醒：以为旋转格转任意角度（固定90°）；以为旋转改变颜色（只改朝向）；以为旋转格永远有用（按需）。总结：旋转格是踩入后让物体旋转 90° 的功能格——改变物体朝向而不改颜色形状，用于目标要求特定朝向时；旋转次数由目标朝向与当前朝向的差决定，路径规划就是安排经过旋转格的时机，与其他功能格（换色/换形）组合实现完整状态匹配。",
'换色格': "什么是换色格、换色格有什么用，是『换色格』vs『颜色改变』的矛盾——换色格（是功能格的一种）、物体（踩入（换色格）、物体（颜色（改变）、变成（目标颜色）、换色格（在（颜色不匹配时用）、目标（要求颜色（换色格（改变颜色）——换色格的真相：换色格=踩入后改变物体颜色的功能格、改变颜色属性（不改变形状朝向）、用于（目标要求特定颜色时）、颜色是状态匹配的一个维度、换色格（与其他功能格组合（实现状态匹配）。真相：①什么是换色格——定义：换色格（是（功能格的一种）、物体（经过（换色格）、颜色（发生变化）、变成（格子的颜色（或指定色）、换色格（只改变（颜色）、不改变（形状朝向）——功能格类型（改变颜色（只改颜色；②换色格有什么用——作用：目标（要求（特定颜色）、物体（颜色不对）、换色格（让颜色（匹配）、比如（目标要红色（物体蓝（过换色格（变红）、换色格（是（颜色的调节器）——颜色匹配目标（换色格调节（匹配；③什么时候用换色格——适用条件：物体颜色（和目标（不一致时）、需要（什么颜色（找（对应换色格）、换色格（有条件（不是乱用）、条件（=目标颜色（需求）、看（目标（选（换色格）——颜色不符时用（按目标需求（有条件；④换色格和其他功能格什么关系——关系：换色格（改颜色（旋转格（改朝向）、换形格（改形状）、维度（各管一个）、状态匹配（=全部维度（都要对）、换色（+旋转（+换形（组合）、顺序（先转后变色（看目标）——各管一维（组合匹配（顺序讲究；⑤怎么规划经过换色格——策略：看（目标颜色）、当前（颜色）、差异、选（对应换色格）、路径（经过（换色格）、颜色（就对齐）、换色格（=颜色的（控制点）——目标-当前颜色差（选对应格（路径经过。误区提醒：以为换色格随机变色（变指定色）；以为换色改变形状（只改颜色）；以为换色格永远要经过（按需）。总结：换色格是踩入后改变物体颜色的功能格——改变颜色属性而不改形状朝向，用于目标要求特定颜色时；换色是状态匹配的一个维度，与其他功能格（旋转/换形）组合、按目标需求选择经过，实现完整的状态匹配。",
'换形格': "什么是换形格、换形格有什么用，是『换形格』vs『形状改变』的矛盾——换形格（是功能格的一种）、物体（踩入（换形格）、物体（形状（改变）、变成（目标形状）、换形格（在（形状不匹配时用）、目标（要求形状（换形格（改变形状）——换形格的真相：换形格=踩入后改变物体形状的功能格、改变形状属性（不改变颜色朝向）、用于（目标要求特定形状时）、形状是状态匹配的一个维度、换形格（与其他功能格组合（实现状态匹配）。真相：①什么是换形格——定义：换形格（是（功能格的一种）、物体（经过（换形格）、形状（发生变化）、圆形（变方形（等）、换形格（只改变（形状）、不改变（颜色朝向）——功能格类型（改变形状（只改形状；②换形格有什么用——作用：目标（要求（特定形状）、物体（形状不对）、换形格（让形状（匹配）、比如（目标要方形（物体圆（过换形格（变方）、换形格（是（形状的调节器）——形状匹配目标（换形格调节（匹配；③什么时候用换形格——适用条件：物体形状（和目标（不一致时）、需要（什么形状（找（对应换形格）、换形格（有条件（按需用）、条件（=目标形状（需求）、看（目标（选（换形格）——形状不符时用（按目标需求（有条件；④换形格和其他功能格什么关系——关系：换形格（改形状（旋转格（改朝向）、换色格（改颜色）、三个（各管（一个维度）、状态匹配（=三个维度（都对）、换形（+旋转（+换色（组合）、顺序（有讲究（看目标）——各管一维（组合匹配（顺序讲究；⑤怎么规划经过换形格——策略：看（目标形状）、当前（形状）、差异、选（对应换形格）、路径（经过（换形格）、形状（就对齐）、换形格（=形状的（控制点）——目标-当前形状差（选对应格（路径经过。误区提醒：以为换形格随机变形（变目标形状）；以为换形改变颜色（只改形状）；以为换形格永远要经过（按需）。总结：换形格是踩入后改变物体形状的功能格——改变形状属性而不改颜色朝向，用于目标要求特定形状时；换形是状态匹配的一个维度，与旋转、换色组合、按目标需求选择经过，实现完整的状态匹配。",
'状态匹配': "什么是状态匹配、状态匹配怎么通关，是『状态匹配』vs『目标判定』的矛盾——状态匹配（是物体状态（和（目标状态（一致）的判定）、物体（有状态（颜色（形状（朝向）、目标（要求（特定状态）、状态一致（=匹配（通关）、状态匹配（是（游戏的通关条件）——状态匹配的真相：状态匹配=物体状态与目标状态一致的判定、状态维度（颜色/形状/朝向）、全部一致（才匹配（通关）、通过（功能格（改变状态（达成匹配）、识别（目标要求（规划（功能格路径）、状态匹配（是通关的目标条件）。真相：①什么是状态——定义：状态（是物体的（属性集合）、颜色（红蓝（等）、形状（圆方（等）、朝向（横竖（等）、位置（坐标）、状态（=物体的（当前属性）、游戏里（物体状态（会被功能格改变）——属性集合（颜色形状朝向（位置）；②什么是状态匹配——定义：状态匹配（是物体状态（与（目标状态（一致）、目标（规定了（要什么状态）、物体状态（=目标状态（匹配成功）、全部维度（都要一致）、颜色（形状（朝向（都对（才算）、状态匹配（=通关的（判定）——物体状态=目标状态（全部维度一致（通关判定；③怎么让状态匹配——达成：状态（不对（就改变）、功能格（改变状态（工具）、颜色不对（过换色格）、形状不对（过换形格）、朝向不对（过旋转格）、顺序（设计（功能格路径）、状态匹配（=规划+执行）——功能格改变状态（按需选格（顺序设计；④状态匹配和功能格什么关系——关系：功能格（是（改变状态的工具）、状态匹配（是（目标）、功能格（改变状态（→匹配目标）、旋转格（改朝向（换色格（改颜色（换形格（改形状）、目标（决定（用什么功能格）、功能格（是手段（匹配是目的）——功能格是手段（匹配是目的（目标决定选择；⑤怎么识别匹配条件——条件识别：先看（目标要（什么状态）、再比（当前状态）、差（哪些维度）、差什么（用什么功能格）、匹配条件（=目标状态（清单）、识别（条件（规划（路径）、状态匹配（=条件识别的（应用）——目标状态清单（对比当前（差什么补什么。误区提醒：以为状态匹配=颜色匹配（全部维度）；以为匹配只要接近（要完全一致）；以为状态匹配靠运气（靠规划）。总结：状态匹配是物体状态与目标状态完全一致的判定——状态维度（颜色、形状、朝向）全部一致才算通关；通过功能格（旋转/换色/换形）改变状态、按目标需求规划路径顺序，状态匹配是通关的目标条件，也是条件识别在游戏任务中的应用。",
}

# 1. DOMAIN 触发词
def find_dict_end(src, dict_name):
    i = src.find(dict_name + ' = {')
    assert i >= 0, f'{dict_name} 未找到'
    depth = 0
    j = src.find('{', i)
    for k in range(j, len(src)):
        c = src[k]
        if c == '{':
            depth += 1
        elif c == '}':
            depth -= 1
            if depth == 0:
                return k
    raise RuntimeError('闭合未找到')

end = find_dict_end(src, 'DOMAIN_SYNONYM_CLUSTERS')
block = ''
for k, triggers in NEW_CLUSTERS.items():
    rows = [triggers[i:i+5] for i in range(0, len(triggers), 5)]
    lines_inner = [', '.join('"' + t + '"' for t in row) for row in rows]
    body = ',\n    '.join(lines_inner)
    block += f'    "{k}": [\n    {body}\n    ],\n'
src = src[:end] + '\n' + block + src[end:]

# 2. DOMAIN_ROUTE
try:
    end_dr = find_dict_end(src, 'DOMAIN_ROUTE')
    dr_block = ''.join(f'    "{k}": "游戏机制",\n' for k in NEW_CLUSTERS)
    src = src[:end_dr] + '\n' + dr_block + src[end_dr:]
except RuntimeError:
    print('DOMAIN_ROUTE 跳过')

# 3. REVERSE_DAILY 答案
i_rd = src.find('REVERSE_DAILY = {')
end_rd = find_dict_end(src, 'REVERSE_DAILY')
rd_zone = src[i_rd:end_rd]
existing = [k for k in NEW_ANSWERS if re.search('"' + k + r'"\s*:\s*"', rd_zone)]
print('已在 REVERSE_DAILY（跳过）:', existing)
ans_block = ''
for k, ans in NEW_ANSWERS.items():
    if k in existing:
        continue
    ans_block += f'    "{k}": "{ans}",\n'
if ans_block:
    src = src[:end_rd] + '\n' + ans_block + src[end_rd:]

open(SRC, 'w', encoding='utf-8').write(src)
import py_compile
py_compile.compile(SRC, doraise=True)
print('语法 OK')

import shutil, hashlib
COPIES = [r'D:\Program Files\2_ai\knowledge-base\semantic_translate.py',
          r'D:\Program Files\2_ai\CommonTrustProtocol\aeis\wisdom\semantic_translate.py',
          r'D:\Program Files\1_ai\lingshu-wisdom\wisdom\semantic_translate.py',
          r'D:\Program Files\3_ai\lingshu-wisdom\wisdom\semantic_translate.py']
h0 = hashlib.sha256(open(SRC, 'rb').read()).hexdigest()[:12]
for c in COPIES:
    shutil.copy2(SRC, c)
print(f'五副本同步 [{h0}]')
