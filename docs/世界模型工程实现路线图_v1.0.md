### 里程碑 4.15：图的 3D 语义——物理信息 → 3D 关系边 ✅ 已完成（game_web.spatial3d · 里程碑 4.15）
- **任务**：一个完整的图像，内部有很多子图，互相有位置、大小、亮度、遮挡等关系；
  3D 关系作为边接入语义时空图
- **方案**（spatial3d.py，全部物理/几何线索，不预设语义）：
  · facing(bbox)：区域明暗渐变方向 → 表面朝向（平面/朝左/朝右/朝上/朝下/斜面）
  · global_light：整图渐变统计 → 全局光源方位（物理：统一光源）
  · occlusion_edges：区域边界 T 交点 → occludes/behind 前后深度序（物理：遮挡）
  · symmetry_edges：以图像中线为轴的左右对称对 → symmetric + 对称轴（生物对称性）
  · support_edges：底边接触 → supports/on（物理：重力方向=下）
  · brightness：区域 L 亮度
- **接入**：st_graph.build_st_graph 扩展——节点加 facing/brightness/depth，
  边加 occludes/behind/supports/on/symmetric；网页③区渲染 3D 语义时空图
  （光源 + 节点三维属性 + 3D 关系边清单）
- **新图实测**：光源=正上；支撑 13 条；对称 1 对；遮挡 T 交点 1 对 + 深度序；
  节点全部带朝向/亮度/深度
- **修复**：MCP server.py 遗留 _sys 未定义 bug（启动即崩，冒烟恢复 29/29）；
  清理残留 MCP 进程（自修改闭环误改 server.py）
- **测试**：test_spatial3d 14/14（朝向/亮度/遮挡T交点/对称/支撑/3D图集成）
  + cnn_segment 45/45 + 网页 23/23 + 冒烟 29/29 + 核心 55/55

### 里程碑 4.13：自适应分割 + 注意力递归细化（MST + LAB + 质量自检） ✅ 已完成（game_web.cnn_segment · 里程碑 4.13）
- **任务**：进一步提高分割效果——按条件/算力递归细化=提高分辨率；从外部到内部
  形成图的信息定义
- **方案**：
  · LAB 感知特征（L 亮度 + a/b 色度分离，max-pooling 彩度保留最强响应）——对光照鲁棒
  · MST 自适应阈值分割（并查集+边排序，合并条件 w < intra + k/size）——大区域容忍
    内部变化、小区域严格，替代全局固定 0.35
  · 注意力递归细化（粗分割 k0=0.5 大片 → 按 info×面积 取 top-N 放大细分 k1=0.2
    细节，算力按条件分配、从外部到内部）
  · 质量自检（info 内部一致性 + boundary 边界强度 + 全局质量分）
- **验收**：新图两阶段分割——粗层：背景/头发(浅蓝大片)/上衣(中蓝大片)/身体；
  细化层：红晕(上部浅红)与局部小簇(中部浅红小簇)自动区分；网页新增第 4 区域
  "CNN 自动语义"：每个语义标签 + 对应局部图（patch_b64 裁剪图）卡片，点击卡片
  在原图红框高亮该区域
- **用户确认（方法论闭环）**：颜色(色度)→浅红、亮度(L)→浅/亮浅、轮廓位置→下部-小，
  三通道共同自动识别出低位区域特征区（下部浅红小区域）——"这就是颜色、轮廓、亮度识别
  的作用"，已固化为测试断言（下部红区自动识别）
- **词补充（用户要求）**：区域签名扩展为 颜色-形状-位置yx-大小 四段全词——
  颜色(LAB 色度)+亮度(L 档)合并于颜色词；形状词(圆点/块状/竖条/横条/散状，由宽高比+
  填充率派生)；位置词两维(上中下×左中右)；大小词(小/中/大)。例：局部小簇=浅红-圆点-中中-小、
  低位区域=浅红-圆点-下中-小、上衣=中蓝-块状-上中-大——全部由特征派生无预设
- **测试**：test_cnn_segment 45/45（LAB 特征/自适应分割/递归细化/质量报告/红晕vs
  局部小簇/下部红区/四段格式/形状词/2D位置词）+ 时空图 13/13 + 网页 21/21 + 冒烟 29/29
  + 核心 55/55

### 里程碑 4.12：卷积自动获得语义（CSPMN 原生神经网络落地） ✅ 已完成（game_web.cnn_segment · 里程碑 4.12）
- **任务**：语义不必提前预设，而是根据 CNN 自动卷积时获得——先看理论中如何设计原生 CNN
- **方案**：cnn_segment.py 按第七章 CSPMN 实现——特征向量场=卷积输出（每像素一个条件
  向量：R/G/B 颜色矩 + 边缘强度 + 方向偏好 + 纹理方差）；feature_segment=按特征向量
  相似度 BFS 分割（区域=条件空间里条件向量相似的像素簇，语义从卷积响应自动涌现）；
  region_prototypes=区域质心 k-means 聚类（条件空间子矩阵=涌现语义类别）；auto_label=
  特征签名自动命名（白/中蓝/中红/暗灰/浅红/浅蓝…，非预设调色板，低饱和按色相倾向）；
  improve_segmentation=特征引导合并（用特征改进分割）。semantics_web /api/segment 增加
  cnn_auto 字段 + 网页"CNN 自动语义"切换（虚线框叠加自动区域 + 涌现类别）
- **验收**：合成图（蓝块/红块/纹理条）特征分割自动分离，命名由特征派生（无预设表）；
  新图（浅蓝头发/蓝色上衣/人物插画）自动涌现特征区域与语义类别，网页可视化
- **新图复核（用户指出旧描述过时）**：新图实际为浅蓝头发 + 蓝色上衣 + 露出胸廓；
  红色内容有两类——head 区红晕（上部大面积浅红）与局部小簇（胸廓左右对称小簇）。CNN
  自动语义已区分：max-pooling 颜色通道（cell 内最饱和像素=最强卷积响应保留，小细节
  不被均值稀释）使局部小簇与肤色分离；标签扩展空间位置/大小词（上部/中部/下部 + 小/中/
  大）——同一颜色类别下的不同内容由空间属性自动涌现：红晕=浅红-上部-中、局部小簇=浅红-
  中部-小（测试断言：上部红区=红晕、中部红区=局部小簇，24/24）
- **理论对照（智能论3.4 第七章）**：卷积核=条件空间匹配器=矩阵点积；层=条件空间层级；
  局部匹配算子可替换（此处用卷积实现）；搜索=矩阵运算；无需反向传播——本里程碑以
  卷积激活自动获得语义，语义不预设、从激活中涌现
- **测试**：test_cnn_segment 24/24（特征场/自动分割/涌现命名/无预设表断言/特征合并/
  真实图像/红晕vs局部小簇空间区分）+ 时空图 13/13 + 网页 21/21 + 冒烟 29/29（81 工具）
  + 核心 55/55

### 里程碑 4.11：CNN 图像处理 + 语义时空图 ✅ 已完成（game_web.cnn + st_graph · 里程碑 4.11）
- **任务**：换新图再试——完全使用语义时空图与原生神经网络；CNN 本身适合图像处理
- **方案**：cnn.py 白箱 CNN（Sobel 边缘/拉普拉斯/方向线卷积核，最大/平均池化，区域特征=