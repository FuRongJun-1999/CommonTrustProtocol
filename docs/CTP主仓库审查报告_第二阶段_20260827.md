# CTP 主仓库审查报告 · 第二阶段（2026-08-27）

> 承接第一阶段（同日）。本阶段目标：**副本收敛 + 历史归档 + 残留清零**。
> 荣指示：大量错误、过时描述、重复副本，放到历史或备份文件夹——这是自举路线的第一步（地基清理）。
> 方法：git mv 保历史可溯；每批执行后跑六域管线测试 + repro_gate 验证零破坏。

## 一、真源判定（证据链）

| 判定 | 证据 |
|---|---|
| **`aeis/wisdom/` 是 wisdom 真源** | `aeis/aeis/api.py::_get_wisdom()` 将 `dirname(dirname(api.py))/wisdom` 注入 sys.path 后 `import wisdom_book`——运行时唯一加载路径 |
| `tools/` = 工具杂物间（835 条目/10.7MB） | patch_round_*×15、gen_*×109、diag/debug/check/analyze/audit/repro 类 ×47、test_*×101 及杂项——绝大多数为一次性历史脚本 |
| `aeis/wisdom/tools/`（86 条目）= 子集镜像 | 与 tools/ 同名文件哈希不一致（code_solidified.json、verifier.py 两处不同） |
| 根 `wisdom/`（31 文件）= knowledge-base 早期遗留 | 第一阶段报告定性为早期工作区副本 |

## 二、新发现的违规案例（本阶段修正对象）

1. **`aeis/wisdom/wisdom_book.py:18` 硬编码**：`AEIS_COPY = r"D:\Program Files\1_ai\AEIS-copy"` 注入 sys.path——跨副本污染的机制性根源（违反六条红线之硬编码禁令）
2. **verifier.py 双版本漂移**：tools/ 版含 savings() 修复（2746021）；aeis/wisdom/tools/ 版仍为旧版——"改副本不同步"红线现行案例
3. **code_solidified.json 三处三版**：tools/=619 条、aeis/wisdom/=396 条、aeis/wisdom/tools/=另值——口径混乱的直接来源
4. **`aeis/wisdom/semantic_translate.py.bak-整卡`**：备份残留入 git

## 三、清理分组与处置

### A 组 · 磁盘垃圾（不在 git，直接删，git 历史 N/A）
- `aeis/build/`（构建产物，.gitignore 已覆盖但磁盘残留，内含整套 wisdom 旧镜像）
- `aeis/aeis.egg-info/`
- 各处 `__pycache__/`

### B 组 · 过时文档归档（git mv → docs/history/）
- 根目录 `共同信任协议_理论版_13.4.1.md / _17.0.md / _18.0.md`（现行为智能论3.3.md 配套 18.x？需标注现行版后归档旧版）
- `aeis/wisdom/semantic_translate.py.bak-整卡`（git rm，历史可溯即可）

### C 组 · tools/ 大扫除（git mv → historical/scripts/）
- 归档：patch_round_*（15）、gen_*（109）、diag_/debug_/check_/analyze_/audit_/repro_issue 等一次性脚本（~50）、不再被引用的 add_/build_/migrate 杂项
- 保留（被 README/docs/运行时引用）：repro_gate.py、self_iterate.py、auto_iterate.py、whitebox_workflow.py、verifier.py、test_verifier*.py、compiler_code_units.py、mini_python.py、mini_compiler.py、code_compose.py、code_solidified.json 等活跃工具与数据
- 判据：逐个 grep 引用（docs/README/互相 import），无引用且属一次性性质即归档

### D 组 · 镜像收敛（最敏感，逐文件裁定）
- 方向：**向真源收敛**——aeis/wisdom/tools/ 中与 tools/ 不一致的文件逐个判定「哪份更新→同步到两处」或「确认为死副本→归档」
- verifier.py：savings() 修复同步至所有存活副本
- code_solidified.json：确定权威份数（第一阶段认定 CTP 内某份为权威 459 单元），其余重建或归档
- AEIS_COPY 硬编码：删除注入逻辑（正源即自身目录）

### E 组 · 描述校正
- README 全量数字复核（73 工具已完成于第一阶段；本批复核剩余引用）
- dsh-memory README「代码(2)」工具表等跨仓库过时描述（另一仓库任务，挂账）

## 四、执行记录

（每批执行后在此登记：动作、范围、验证结果、提交号）

| 批次 | 内容 | 验证 | 提交 |
|---|---|---|---|
| B1 | 磁盘垃圾：aeis/build/(24.5MB) + egg-info + 11×__pycache__ 删除 | N/A（未入 git） | — |
| B2 | 文档归档：共同信任协议 13.4.1/17.0 → docs/history/；bak-整卡 删除（未跟踪） | git status 复核 | 本提交 |
| B3 | tools/ 大扫除：762 个一次性脚本 → historical/scripts/（git mv 761 + mv 1，保留白名单 69 条目含依赖营救 8 个） | 六域 686/687 · 编译器 272/272 · P线 240/240 零破坏 | 本提交 |
| B4 | 硬编码修正：wisdom_book.py 删 AEIS_COPY 注入；DEFAULT_DB 改模块同目录 cloud 库（运行时本就显式传参，零影响面） | import 验证 DB 指向真源 | 本提交 |
| D 组（下批）| 镜像收敛：aeis/wisdom/tools 与 tools 不一致文件逐个裁定；verifier/code_solidified 同步 | 待执行 | — |

## 五、不做的事（防过度清理）

- 不删 `plugins/dsh-memory`（工程层独立仓库的源头映射）
- 不动 `testsets/migration`、`memory-seed`、`seed_knowledge`（发布物组成部分）
- `aeis/dist/aeis-0.4.0*.whl` 保留（当前分发通道）；0.3.0 旧 wheel 待确认后处理
- `aeis/models/`、GPT_SoVITS（语音栈）暂不动（视觉惰性化相关决策另行处理）


## 六、第二轮复扫（2026-08-27 续）

**方向反转判定**：六域单元库的真实活跃源是 `tools/`（681 单元，8-26 理论修正落点；六域合计恰为 dsh 端报告数字），`aeis/wisdom/` 内同名文件是 8-25 过时快照（459 单元）且被 wheel 0.4.0 打包发布——**wheel 用户拿到的是缩水知识库**，属发布完整性问题。
同时 code_compose.py 构成**双向分叉**（aeis/wisdom 版 8-26 修 consolidate_cycle；tools 版 8-25 有 SB-SPEC 注释规范改造）——机械覆盖将丢失修复。

已执行（本批）：
| 动作 | 说明 |
|---|---|
| aeis/wisdom/tools/ 整体归档 | 86 条目纯镜像 → historical/duplicate_snapshot/wisdom_tools/ |
| 根 wisdom/ 整体归档 | knowledge-base 遗留（engine×3 + 识别卡/理论等 31 文件）→ historical/duplicate_snapshot/wisdom_root_* |
| 根目录 cosyvoice_tts.py / voice_thread.py 归档 | 与 aeis/ 内版本同内容且零引用 |
| 测试路径硬编码确认 | 四套自举测试均硬编码绝对路径指向 tools/（红线案例，待 D 组改相对定位） |

**挂账 · D 组镜像合并工程**（独立任务，非清理可顺手完成）：
1. 六域 units 单向同步 tools→aeis/wisdom（消除 wheel 缩水发布）
2. code_compose.py 双向合并（consolidate_cycle 修复 × SB-SPEC 规范两保全）
3. ccg.py 999 行分叉裁定、verifier.py 同步 savings 修复至全部存活副本
4. code_solidified.json 权威裁定（619 vs 396 vs 已归档份）
5. 测试 sys.path 相对化（消硬编码）
6. aeis/api.py 顶层残留 vs aeis/aeis/api.py 双向漂移裁定
7. core.py:4102 roleplay_web.py:545 的 knowledge-base 本机硬编码路径处理
