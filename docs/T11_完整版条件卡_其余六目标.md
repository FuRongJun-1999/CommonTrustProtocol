# T11 梯队二 · 完整版条件卡：目标 1/3/4/5/6/7（2026-08-28）

> 模板同编译器卡（docs/T11_完整版条件卡_中文编译器.md）：
> 功能面清单 / 验证用例 / 限制显式 / 当前状态。
> 每卡的目标：让「完整」可判定——清单未勾完 = 未完整。

---

## 目标 1 · 中文编程语言（P 线 Mini-Python）

### 功能面
- F1 表达式：算术/比较/逻辑、优先级、变量
- F2 语句：赋值/若则（else 可选）/当循环
- F3 函数：定义/递归/返回
- F4 数据结构：列表/字典/索引
- F5 字符串方法：upper/split/startswith/endswith/strip（T4 族）
- F6 内置：range/len/sum/print/abs/min/max/sorted/int/float/str/list
- F7 复合：列表字典组合操作（排序+去重+统计）

### 验证（现状：240/240 + T4 19/19 已覆盖核心；补齐清单）
- 缺口 V-P1：错误处理语义（除零/未定义变量的显式报错形态统一）
- 缺口 V-P2：列表字典嵌套组合用例（排序+去重+统计一条龙）
- 缺口 V-P3：字符串方法族全覆盖用例（contains/endswith/join/startswith/strip）

### 限制显式
无类/无异常捕获 try/无模块导入/无标准库 IO。

**状态**：功能面 7/7 组有实现，验证缺口 3 组——完整度 ~70%（教学完整）
**清单化后修正**：此前一轮估 35% 偏低——P 线 T4 族补齐后实际更高。

---

## 目标 3 · 中文分析器/解释器

### 功能面
- F1 类型推断：赋值→数值/文本/布尔；冲突→混合标记（✓ 校准⑬）
- F2 类型检查：未推断/混合类型使用 → 编译期拦截（✓ 校准⑭）
- F3 符号表转储：变量/类型/作用域完整视图
- F4 调用图：函数间调用关系提取
- F5 数据流：变量定义-使用链

### 验证
- V-A1 类型冲突程序 → 编译期拦截 ✓
- V-A2 符号表转储含全部变量与类型
- V-A3 调用图含全部函数边
- V-A4 未定义使用 → 拦截

### 限制显式
静态单遍分析；不支持跨程序文件；类型系统为三基础类型。

**状态**：F1/F2 ✓，F3-F5 未实现——完整度 ~40%。

---

## 目标 4 · 操作系统（Linux 复现·教学完整）

### 功能面
- F1 进程：创建/回收/调度（工作窃取已 demo）/状态机
- F2 调度策略：优先级/时间片/窃取可选
- F3 内存：分配/回收（教学模拟）
- F4 文件系统：创建/读写/删除（教学模拟）

### 验证
- V-O1 调度演示多进程交错执行
- V-O2 窃取负载均衡统计
- V-O3 文件创建读写删除往返
- V-O4 进程状态机非法流转拒绝

### 限制显式
教学模拟（无真实硬件/内核）；单机；并发用协作式模拟。

**状态**：调度 demo ✓——完整度 ~20%。

---

## 目标 5 · 网络协议栈

### 功能面
- F1 连接：握手（三次）✓ demo
- F2 分帧：粘包/半包处理 ✓ demo
- F3 会话：状态维护 ✓ demo
- F4 滑动窗口 ✓ demo
- F5 重传：超时/确认重发
- F6 校验和：数据完整性
- F7 拥塞控制：窗口调节

### 验证
- V-N1 粘包/半包/跨包混合流解析
- V-N2 丢包→超时重传→去重
- V-N3 校验和错包丢弃
- V-N4 窗口滑动边界

### 限制显式
教学模拟（无真实射频/网卡）；协议状态机层完整为目标。

**状态**：F1-F4 ✓ demo，F5-F7 未实现——完整度 ~50%（协议状态机层）。

---

## 目标 6 · 图数据库

### 功能面
- F1 建图：节点/边/属性 ✓ demo
- F2 查询：最短路径 ✓ demo / 邻域 / 度分布
- F3 持久化：存盘/加载往返
- F4 条件路由图同构：知识图 ↔ 图数据库（第 4 阶段积累直接落地）
- F5 索引：节点/边快速定位
- F6 事务：多操作原子性

### 验证
- V-G1 建图→查询→删除往返一致
- V-G2 千节点图最短路径性能
- V-G3 条件路由图（3971 节点）全量导入查询
- V-G4 崩溃后重载一致（持久化）

### 限制显式
教学完整（单机/无分布式）；查询 API 为白箱单元级。

**状态**：F1/F2 ✓ demo，F3-F6 未实现——完整度 ~35%（同构优势最大域）。

---

## 目标 7 · 蓝牙互联网

### 功能面
- F1 蜂群组网：连接/断连 ✓ demo
- F2 服务发现：设备能力声明
- F3 配对：信任建立
- F4 消息路由：多跳转发

### 验证
- V-B1 组网拓扑收敛
- V-B2 服务发现完整
- V-B3 配对信任链
- V-B4 多跳消息到达

### 限制显式
教学模拟（模拟射频层）；蜂群拓扑为目标形态。

**状态**：demo ✓——完整度 ~15%（依赖射频模拟层设计）。

---

## 汇总（清单化后）

| 目标 | 一轮估 | 清单化判定 | 最大缺口 |
|---|---|---|---|
| 1 编程语言 | ~35% | **~70%** | 验证用例补齐 |
| 3 分析器 | ~30% | **~40%** | F3-F5 转储/调用图/数据流 |
| 4 OS | ~15% | **~20%** | 文件系统/内存 |
| 5 网络 | ~15% | **~50%** | 重传/校验/拥塞 |
| 6 图库 | ~25% | **~35%** | 持久化/同构落地 |
| 7 蓝牙 | ~10% | **~15%** | 射频模拟层设计 |
| 2 编译器 | ~40% | 见编译器卡 | V2 多参/V6 组合 |

**清单化的价值实证**：目标 1 从估 35% 修正为 70%——主观估计系统性
低估已有积累；而 4/5/7 的低分是真实的（功能面未动工）。
