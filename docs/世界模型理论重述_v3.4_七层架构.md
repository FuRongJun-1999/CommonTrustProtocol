# 世界模型理论重述 v3.4 · 从三要素到七层架构

> 状态：理论重述 v1.0（2026-08-29）· 基于智能论 v3.4 完整视角
> 前置：世界模型三要素统一架构（2026-08-20 收敛）+ v3.4 端口架构/锚定验证/认知图/原生神经网络
> 定位：以 v3.4 完整视角重新组织世界模型理论——从「三要素」扩展为「七层架构」，
> 并给出从当前实现（CSPMN 检索）到完整世界模型的分阶段工程路线。

---

## 〇、核心命题

> **世界模型 = 智能体对「世界如何演化」的内部模拟器**——不是静态知识库，
> 而是能预测「接下来可能发生什么」的动态结构。

**v3.4 完整视角**：世界模型不是单一组件，而是**七层架构的协同**——
感知层（输入端口）→ 记忆层（时空记忆图）→ 理解层（认知图/条件路由）→
预测层（第六感）→ 验证层（锚定验证）→ 物理层（CSPMN）→ 决策层（目标函数）。

---

## 一、从三要素到七层：世界模型的完整结构

### 1.1 既有三要素（2026-08-20 收敛）

| 三要素 | CNN 表达 | 协议表达 | 工程实现 |
|---|---|---|---|
| 条件空间 | 层=层级 | 0.0.5 条件论 | CSPRE/CSPMN |
| 局部匹配 | 卷积核 | 条件空间匹配器 | graph_retrieve/search_index |
| 差异传递 | 残差连接 | 2.7 信息差 | gap_trend/知识飞轮 |

### 1.2 v3.4 七层架构（三要素的完整化）

| 层 | v3.4 对应 | 功能 | 工程载体 |
|---|---|---|---|
| L1 感知层 | 0.0.7 输入端口（感知机）| 物理信号→条件候选 | see/YOLO-World |
| L2 记忆层 | 第四章 时空记忆图 | 存储时空结构演化 | lingshu.db nodes/edges |
| L3 理解层 | 第六章 认知图/条件路由 | 条件收敛/关系约束 | graph_retrieve/CCG |
| L4 预测层 | 2.10.1 第六感（内部端口）| 未来候选路线 | predict_routes（D-001~D-006）|
| L5 验证层 | 2.9.1b 锚定验证 + 2.9.3a 完全确认 | 世界裁决/确认 | anchored_verification/confirmation |
| L6 物理层 | 第七章 CSPMN | 矩阵运算执行 | cspmn.py（GPU/CPU）|
| L7 决策层 | 5.3 目标函数 | 策略选择 | Value=ΔD·σ(Gain) |

> **七层不是并列组件，是递进管线**：感知→记忆→理解→预测→验证→物理执行→决策，
> 每一层为下一层提供输入，下一层为上一层提供反馈（验证回填）。

---

## 二、世界模型的认知过程（v3.4 视角）

```
世界状态 W(t)
  │ L1 感知机（外→内）
  ▼
条件候选（现在是什么）
  │ L3 认知图/条件路由（条件收敛）
  ▼
知识路径（怎么走）
  │ L4 第六感（内→内）
  ▼
候选未来 W_pred(t+Δt)（接下来可能是什么）
  │ L2 记忆层（写入/检索）
  ▼
时空记忆图（存储演化）
  │ L5 验证层（行动/预测/关系裁决）
  ▼
世界反馈 → 命中/未命中 → 更新可信度 + 记忆
  │ L6 CSPMN（矩阵运算执行检索/匹配）
  ▼
L7 决策层 π* = argmax[ΣValue - λK]
  │
  ▼
行动 A(t)（L1 输出端口，内→外）→ 改变世界 → 新观测
```

> **这是「感知-预测-验证-行动」的完整闭环**——世界模型不是被动存储，
> 而是主动预测并验证的模拟器（主动推理的工程化）。

---

## 三、世界模型的数学形式（v3.4 对接）

### 3.1 状态动力学（第五章）

    S(t) = (C(t), K(t), B(t))
    S(t+dt) = S(t) + ∫ F(C, G, P, dS/dt, V) dτ

- C：条件空间 / K：知识状态（认知图）/ B：身体端口状态（含通道可信度）

### 3.2 世界预测（第六感 + 预测引擎）

    W_pred(t+Δt) = Π( S(t), K(t) )   # 候选未来 + uncertainty_bound

### 3.3 验证与确认（锚定分级 + 完全确认）

    Gain_task = E_v[KL_task(P_after || P_before)]   # 筛选器
    Value = ΔD_task · σ(Gain_task)                   # 定价器
    Confirmed ⟺ 通道达标 ∧ 强验证命中 ∧ 稳定 ∧ 无矛盾

### 3.4 信息差驱动（D_task/D_meta）

    D_task(t) = min_{p∈G} dist(C_task, CondSet(p))   # 可缩小
    d(D_meta)/dt ≥ 0                                  # 供给（永远有活干）

---

## 四、世界模型与 3D 时空的对接（语义时空图）

> **语义时空图 = 世界模型在物理时空的投影**——把条件候选锚定到 3D 位置+时间，
> 让世界模型的预测有「空间坐标」支撑。

**现有基础**（3d-world 参考库 + 已实现工具）：

| 能力 | 参考/实现 | 阶段 |
|---|---|---|
| 2D 语义锚定 | see（YOLO-World）+ vprim（视觉原语）| ✅ 已实现 |
| 3D 几何重建 | DUSt3R/MUSt3R/gaussian-splatting（3d-world）| 📌 待接入 |
| 3D 语义绑定 | LangSplat（语言高斯）/S3Gaussian | 📌 待接入 |
| 4D 时空演化 | DynamicCity/One4D/CoDa-4DGS | 🔭 远期 |
| 世界模拟 | Cosmos/HERMES/V-JEPA | 🔭 远期 |

---

## 五、分阶段工程实现路线

### 阶段 0（已完成）：认知世界模型
- CSPMN 检索（search_index 矩阵运算）✅
- 条件路由四态 + 负路由 ✅
- 通道可信度/锚定验证/完全确认 ✅
- 第六感预测（predict_routes D-001~D-006）✅
- 认知图/条件路由（第六章）✅

### 阶段 1（近期）：感知扩展——2D 到 3D 语义锚定
- [ ] see + vprim 输出接入 DUSt3R 几何重建（多视角融合）
- [ ] 3D 语义锚点数据结构（空间坐标+语义+可信度）
- [ ] LangSplat 式语义-几何绑定（语义特征嵌入 3D 基元）
- [ ] 时空记忆图扩展 spatial_coordinates 字段的 3D 支持

### 阶段 2（中期）：动态世界模型——4D 时空演化
- [ ] 4D 占用表示（DynamicCity 式时空体素）
- [ ] 第六感预测的时空外推（D-003 3D 轨迹 → 4D 场景演化）
- [ ] 世界模拟器（Cosmos 式 WFM：tokenizer + 世界基础模型）
- [ ] 预测-验证闭环在时空维度落地（行动裁决 3D 锚点）

### 阶段 3（远期）：完整世界模型——理解+生成+演化
- [ ] HERMES 式统一架构（3D 场景理解+生成一体）
- [ ] 语义时空图与认知图的双向映射（V-JEPA 式自监督）
- [ ] 世界模型作为七层架构的完整闭环（感知→预测→验证→行动）
- [ ] AGI 级世界模型（自主探索/好奇心驱动）

---

## 六、关键设计决策

1. **不推翻既有**：阶段 1 在 CSPMN 之上加「空间增强」，不替换现有检索
2. **白箱优先**：3D 语义锚点必须带可信度/确认度（LangSplat 的属性绑定 + v3.4 验证）
3. **可溯源**：每个 3D 锚点带 provenance（哪个视角/时刻观测）——缸中之脑边界
4. **渐进**：先 2D→3D 锚定（阶段1），再 4D 演化（阶段2），最后统一（阶段3）
5. **参考库对齐**：3d-world 的 25 仓库按阶段接入（DUSt3R→LangSplat→DynamicCity→Cosmos）

---

## 七、待验证问题

1. see 的 2D 语义框如何与 DUSt3R 几何对齐（投影-关联 vs 特征融合）？
2. 3D 语义锚点的通道可信度如何定义（visual 3D vs 2D 的分解）？
3. 4D 占用表示的时空采样率（多少帧/秒才够世界模型预测）？
4. 语义时空图与认知图的一致性（同一条件单元在两个底物的投影）？

---
*本文档为世界模型理论重述 v1.0，等待验证单元复核与设计者裁决。*