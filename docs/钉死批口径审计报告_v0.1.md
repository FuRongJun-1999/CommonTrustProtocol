# 钉死批口径审计报告 v0.1（2026-08-31 心跳）

> 依据：docs/概念钉死批_GPT四点评审_v0.1.md 承诺的「既有实现口径审计」。
> 范围：条款 1/2/4（条款 3 已于 c7ac6e4b 单点化收官，本报告不重复）。

## 总判定

| 条款 | 审计对象 | 判定 | 处置 |
|---|---|---|---|
| 1 dist_C=cov | `aeis/wisdom/navigate.py::_accept_by_score` | **PASS（一致）** | 补条款引用注释（本批） |
| 2 条件代数/哈希分离 | 主仓库 Python 侧 | **N/A（未到实现阶段）** | 规范已钉，四件套实现时执行 |
| 4 二阶导落位 | `core.py::attention_shift` | **已实现（一致）** | 发现持久化缺口→待办 |

## 条款 1：dist_C 离散=cov 对齐 —— PASS

- `_accept_by_score`（navigate.py:125）score+coverage 双维门槛，
  docstring 完整记录 24 题数据驱动规则（导航种子 ≥3 且 cov≥0.15 /
  知识卡 ≥4 或 =3 且 cov≥0.22）——cov 即钉死条款 1 定义的
  `dist_C := 1 - cov` 离散实现，判别力已实证（T8 复测 87.5%，零误杀零漏放）。
- 连续条件偏序（海拔>2000m ⊂ 高原地区）未混入实现——与「留 v2」口径一致，
  无过度设计。
- 本批处置：docstring 补钉死批条款引用（口径溯源）。

## 条款 2：条件代数与哈希分离 —— N/A（无既有违规）

- 主仓库 Python 侧无 cond_delta XOR 合并实现（KNode/CEdge 四件套属
  CRG-DB 数据模型，Rust 设计稿阶段+Python 侧未建）——不存在可违反的现场。
- `tools/route_cache.py` 的 sha256 指纹是**库变更检测**（fingerprint 失效），
  不是条件键代数——与条款 2 无冲突。
- 处置：条款 2 四则（集合语义/否定分离/依赖走边/哈希预筛路径裁决）
  已钉入规范，Python 四件套实现批次按条款执行并复跑本审计。

## 条款 4：二阶导落位 —— 已实现（先于钉死批）

- **发现**：`core.py::attention_shift`——「二阶偏好转向信号 d²D_norm/dt²
  （PROP-EMO-DIRECTION-002 共用计算）」已在策略层（attention_policy）
  实现。GPT 建议「情绪=二阶变化终于有地方落」——主仓库先于该建议落地，
  且落位与钉死批条款 4 完全一致：不入库、策略层从 D 序列现算。
- 一阶：`gap_trend`（D_norm 斜率）心跳常态运行 ✓。
- **缺口（待办）**：D 序列 `_gap_history` 仅内存 100 条（core.py:4316），
  不落库——跨进程重启后 d² 断链，长程趋势不可回放。
  处置建议：D 序列小表持久化（ts/d_norm 滚动窗口），随四件套批次落。

## 附：条款 3 收官记录（上批）

统一时间核 cred() 单点化 c7ac6e4b——四调用点收口+数值对拍 4/4+
回归 55/55+E5 lint PASS（引擎侧就地实现清零）。
