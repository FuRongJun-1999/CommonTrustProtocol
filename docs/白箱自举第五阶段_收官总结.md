# 白箱自举第五阶段 · 收官总结（3D 时空视觉闭环 + 代码编写能力深学）

> 阶段目标：①3D 时空视觉闭环（感知↔语义双向 + 自校验）②代码编写能力深学（codegraph 参考）
> 文档先行 → 逐轮工程验证 → 判定项核验 → 收官。

## 一、目标① 3D 时空视觉闭环 ✔（v1/v3/v4/v5/v6）

| 环节 | 交付 | 证据 |
|---|---|---|
| 感知→3D | vision_3d 立体视差→深度→点云（已有）+ scene_graph 点云聚类→场景图 | 闭环 8/8 |
| 语义→3D | world3d_render 场景图→3D 世界（WORLD3D 同款相机 fov/yaw/pitch）→透视渲染 | 闭环 8/8 |
| 空间自校验 | spatial_closure 中心对齐/深度关系/尺寸一致 → **综合 0.989** ≥0.8 | 闭环 8/8 |
| 时间维度 | spacetime_3d 多帧双目→跨帧追踪→3D 轨迹（方向/速度/位移/一致性） | 轨迹 6/6 |
| 记住→问答 | spatial_qa 3D 轨迹→灵枢记忆（spatial3d 标签）→5 型问答（方向/速度/距离/轨迹/静止） | 问答 11/11 |
| 时间维自校验 | spacetime_projection 2D↔3D 两视图互证（方向映射 15 组合+速度互验 7% 误差） | 互校验 8/8 |
| 主路由接入 | chat_engine 时空词表并入 3D 词 → 3D 问答自动走感知通道（未命中诚实回落） | 3D进路由 5/5 |
| 灵枢对接 | WORLD3D add/render 冒烟 ✔；3D 事件写入真实灵枢库（node_482f3e28）✔ | 冒烟 |

## 二、目标② 代码编写能力深学 ✔（v2/v7/v8/v9）

| 层 | 交付 | 证据 |
|---|---|---|
| 学习笔记 | codegraph-ref 架构提炼（crates 解析器矩阵/统一 IR/类型化图模型/图算法/可插拔存储→白箱同构映射） | docs |
| 单文件理解 | codegraph_white：AST→统一 IR→调用图→影响分析（BFS 逆向）→Tarjan SCC 环检测 | 11/11 |
| 仓库级理解 | analyze_repository（多文件 IR 带归属）+ 依赖树 + 跨文件调用定位 + 仓库统计 | 7/7 |
| 跨文件影响分析 | impact_analysis_repo：改函数→跨文件调用面（caller 带文件定位+深度） | 6/6 |
| 多语言 | multilang_ir：Python（ast 完整）+JS/Rust（轻量正则）→统一 IR 复用影响分析 | 12/12 |

## 三、关键发现与诚实校准

1. **真实库召回淹没**：3D 事件写入真实灵枢库成功（importance 0.8），但模糊召回被
   importance=1.0/万级访问的学科卡淹没——隔离库（:memory:）11/11 良好，真实库需
   精确检索策略（标签/ID 直查）。如实记录，不假装无缝。
2. **JS/Rust 轻量提取诚实标注**：light=True，Rust `d.split().collect()` 误抓 collect 如实标注。
3. **并行化诚实校准**（第四阶段延续）：轻负载 GIL 0.79x 如实记录，重负载 5.71x。

## 四、版本链（v1-v9）

v1 235a64a+62303dd（闭环+导入修复+视觉文件补齐）→ v3 f040844（3D 轨迹）→ v4 e721c4a（3D 问答）
→ v5 e66312a（2D↔3D 互校验）→ v6 c4b167b（3D 进主路由）→ v7 a432899（仓库级分析）
→ v8 0067093（跨文件影响分析）→ v9 77c89df（多语言）

## 五、全局测试全绿（第五阶段 7 套）

闭环 8/8｜轨迹 6/6｜问答 11/11｜互校验 8/8｜3D进路由 5/5｜仓库分析 7/7+6/6｜多语言 12/12
+ 第四阶段回归：白箱纯度 7/7、常识域 18/18、并行化 8/8、感知 8/8、3D视觉 4/4、stcnn 15/15

## 六、后续方向（下一阶段候选）

1. 真实库精确召回（3D 感知记忆按标签/节点直查，绕开学科卡淹没）
2. 多语言仓库级分析（analyze_repository 支持 JS/Rust 目录）
3. 3D 感知与 lingshu_see（真实图像目标检测）融合——合成→真实图像过渡
4. codegraph_white 与灵枢知识图双向映射（代码图 ↔ 条件路由图）
