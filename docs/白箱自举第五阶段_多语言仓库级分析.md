# 白箱自举第五阶段 · 多语言仓库级分析（混合语言仓库理解）

> 文档先行 → 工程验证。合并两条线：仓库级分析（analyze_repository/依赖树/跨文件调用/
> 影响分析）× 多语言 IR（multilang_ir：Python ast 完整 + JS/Rust 轻量）——
> **混合语言仓库**（.py/.js/.rs 共存）的统一理解。

## 一、设计

```
仓库目录（混合语言）
  ├─ analyze_repository v2：按扩展名分流 extract_ir（py→ast / js→正则 / rs→正则）
  ├─ 依赖树：本地模块判定扩展到 .js/.rs（模块名 → 文件 stem）
  ├─ 跨文件调用：函数名全局索引跨语言（main.js 调 utils.py 的 parse → 定位）
  └─ 影响分析：改 utils.py parse → main.js 调用面（跨语言波及）
```

## 二、修改

| 文件 | 改动 |
|---|---|
| codegraph_white.py | analyze_repository 按扩展名分流到 multilang_ir.extract_ir（延迟 import 防循环）；build_dependency_tree 本地模块判定扩展多扩展名 |
| 新增 test | test_codegraph_multilang_repo.py |

## 三、判定标准

1. 混合仓库（utils.py + lib.rs + main.js）→ 统一仓库 IR（函数带语言/file 归属）✔
2. 依赖树：main.js → utils.py（跨语言导入）✔
3. 跨文件调用：main.js 调 parse → 定位 utils.py（跨语言）✔
4. 影响分析：改 utils.py parse → main.js 函数在调用面（跨语言波及）✔
5. 测试全绿 → 提交 → 五副本同步
