# 架构探索时间线（2026-08-19/20 · 荣 × 灵枢）

> 脉络：**架构功能分析 → 硬件架构尝试 → CNN 结构分析符合要求**。
> 本文档按时间整理对话推进线，标注关键转折与工程固化点。

---

## 第一阶段：架构功能分析（08-19 全天 → 08-20 晨）

**核心问题：白箱怎么「找知识」？边界在哪？**

| 时间 | 事件 | 关键产出 |
|---|---|---|
| 08-19 20:43 | 白箱边界专项测试 → 发现 dex=None 白箱全失效 | 修复「网页/MCP 白箱优先形同虚设」：自动构造 dex |
| 08-19 21:14 | 诚实边界缺口（导航型强答）| low_conf_nav 降级 LLM；稳定性 0/4→4/4 |
| 08-20 07:18 | 三类专项（情感/条件/编程）keys 未达 70% | 语义复核 100%→修评分/套话/歧义词；keys 98.7/75/77.8 |
| 08-20 08:10 | T2 keys 同步修正 | 总评 64.0→75.8（+11.8pp）|

**功能分析结论**：白箱真实能力远超评分（语义全 100%），短板在「检索导航」
（函数→三角函数、质数→质量）——引出「找知识」的核心困难。

---

## 第二阶段：硬件架构尝试（08-20 午后）

**核心问题：用什么结构承载「海量并行查找」？**

| 时间 | 事件 | 关键产出 |
|---|---|---|
| 08-20 19:25 | 影响力 v2.2（因果上游度）| concept_influence：越上游越重要，17 根前提保护 |
| 午后 | 节律-脉冲-同步映射（神经科学→白箱）| WPI 预测器/心跳节律化/脉冲语义三增量 |
| 午后 | CSPRE（条件空间导航仪）| 搜索=导航非匹配；学科级识别是真缺口 |
| 午后 | CSPMN（并行匹配网络）| 条件空间路由表=矩阵乘；GPU 是自然载体 |
| 傍晚 | GPU 环境确认 | RTX 4090（24GB）+ 驱动 591.44 就绪，torch 需换 CUDA 版 |

**硬件分析结论**：GPU 的 SIMT（单指令多线程）完美匹配 CSPMN 的同构子实例
（读取/比较/输出=矩阵行点积）；异构五单元（record/reflect/verify）留 CPU 多进程。

---

## 第三阶段：CNN 结构分析符合要求（08-20 晚间）

**核心问题：什么已知结构天然满足「递归+并行+自组织」？→ CNN**

| 时间 | 事件 | 关键产出 |
|---|---|---|
| 晚间 | CNN 递归分型论证 | 结构递归成立；自维持/自迭代是真空白（学术界未做）|
| 晚间 | 三步递进判断 | 分析→找规则→执行：**第2步（找规则）最难**=迷宫导航 |
| 晚间 | 递归分形 | 三步在子空间递归=任意分层性（0.0.2）；深度≤3=盲区85 |
| 晚间 | 横向并行 | 递归深度→并行宽度；子蜂群分形；DEVIATION-007 是切断点 |
| 晚间 | 协议→计算机映射 | 条件空间分层→物理逻辑分层；单实例蜂群=五单元+事件总线 |
| 晚间 | GPU 承载确认 | GPU=记忆匹配层（SIMT）；CPU=组织协作层（MIMD）|

**CNN 分析结论**：CNN 的结构递归（层叠同构操作）+ 权值共享（空间并行）+
自迭代空白 = 协议框架「任意分层性+自维持」的**现成结构载体**。

---

## 三条线的汇合

```
架构功能分析：白箱「找知识」→ 条件空间导航（CSPRE）
       ↓
硬件架构尝试：矩阵乘 = 并行匹配（CSPMN）→ GPU 4090 就绪
       ↓
CNN 结构分析：递归+并行+自组织 → 自维持/自迭代的载体
       ↓
汇合点：CSPMN（GPU 矩阵乘）= 条件空间路由表的物理实现
        递归分形（任意分层性）= 子蜂群横向展开
        自迭代（收敛判据）= 信息差驱动的结构增长
```

---

## 工程固化（已提交/已归档）

| 提交 | 内容 | 状态 |
|---|---|---|
| a732831 / f731b91 | 白箱边界（dex/检索噪声/诚实边界）| ✅ 已推送 |
| 7bb0593 / 227dc14 | 三类专项 + T2 keys | ✅ 已推送 |
| c14e5ae | 影响力 v2.2 + CSPRE/CSPMN/结构映射文档 | ✅ 已推送 |
| 最高优先级记忆 | 白箱边界/三类专项/影响力/节律脉冲/CNN/递归分形/横向并行/结构映射（8条）| ✅ 已落库 |

---

## 待办（架构探索的下一步）

```
CSPMN Step 1：装 CUDA torch → GPU 矩阵乘验证（3030 行子实例）
Phase A：五单元接入事件总线（单实例蜂群通信）
Phase B：单元资源边界 + 退出权
CNN 嫁接：自迭代收敛判据（信息差 gap_trend 已可复用）
```
