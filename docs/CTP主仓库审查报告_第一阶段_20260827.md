# CTP 主仓库审查报告 · 第一阶段（2026-08-27）

> 原则：**CTP 是唯一主仓库（真源）**，其他（site-packages / knowledge-base / lingshu-wisdom / dsh-memory）都是副本、同步或阉割子内容。本报告以 GitHub 存档为基准，记录审查发现，供第二阶段清理执行。

## 一、审查范围与基准

- 真源：`D:\Program Files\2_ai\CommonTrustProtocol`（git main，与 origin/main 同步）
- 对照副本：site-packages（运行环境）、knowledge-base（早期智慧之书工作区）、lingshu-wisdom（落后发布版）、dsh-memory（阉割适配插件）
- 复核方式：SHA256 三边对比 + git 历史追溯

## 二、确认无问题（三边一致）

| 文件 | 结论 |
|---|---|
| `aeis/aeis/*` 全部 50 个 .py | CTP = SP 完全一致（核心库零污染） |
| `verify_answer.py` | CTP = KB = SP 一致 |
| `wisdom-book-cloud.db` | CTP = SP 同源（3048 节点/2948 边；hash 差异仅为运行时 set_meta 无害写入） |
| `code_solidified.json` | CTP = 238,875B（459 单元完整版，git 20+ 轮累积）✅ 权威 |

## 三、site-packages 被污染（需恢复：CTP → SP）

**根因**：此前 `pip install --force-reinstall lingshu_wisdom-0.2.0.whl`（旧 wheel 含 lingshu-wisdom 旧副本）覆盖了 site-packages 的正确版本。

| 文件 | CTP 权威 | SP 当前污染 | 丢失的修复 |
|---|---|---|---|
| `semantic_translate.py` | 2,105,446B（v1.45） | 2,062,146B | 矛盾键放宽 300→500、核心段第三通道恢复（v1.44/1.45） |
| `chat_engine.py` | 92,597B（v1.43） | 84,486B | 情感域/意见域路由（_assemble_emotion_reply/_opinion_reply，v1.42/1.43） |
| `code_compose.py` | 33,551B | 31,995B | 41 行功能增量 |

**验证依据**：CTP 这三个文件 = knowledge-base（新版），SP = lingshu-wisdom 旧副本。

## 四、主仓库缺失的运行文件（SP/KB 有，主仓库 git 从未有）

| 文件 | 大小 | 性质 | 处理建议 |
|---|---|---|---|
| `pattern_separation.py` | 8,814B | H3 海马体模式分离，**MCP 已注册工具**，KB=SP 一致 | ✅ 补入主仓库（运行必需） |
| `scene_reconstruction.py` | 5,890B | H4 情景重构，KB=SP 一致，server 未注册 | 待定 |
| `danmaku_bridge.py` | KB 4,441 vs SP 4,365 | 弹幕桥，server 未注册 | 待定 |
| `merge_subgraph.py` | 7,590B | 仅 SP 有，他处无 | 待定 |
| `chat.html` | 7,192B | 普通人对话网页（SP 独有） | 待定（见六） |

## 五、MCP 工具数核实

- **实际工具数 = 73**（运行时 `_tools()` 实测，SP 与 CTP 一致）
- README 写「71」→ **过时**，需更正为 73
- 注意：`code_compose`/`code_qa` **已从 server.py 移除**（73 工具中无代码工具），但 dsh-memory README 工具表仍宣称「代码（2）」→ 跨仓库过时描述

## 六、错误修改案例（绝不能入主仓库）

**SP `wisdom_cloud.py`（474 行 vs CTP 276 行）**：
- 新增「普通人对话」端点 `/chat` + `/chat.html`（H5 聊天式第一智能入口）
- 含硬编码：`sys.path.insert(0, r'D:\Program Files\2_ai\knowledge-base')` ← **本机路径残留**
- git 全历史确认：`/chat` 端点与 `chat.html` 从未入库 → 本机调试残留
- 处理建议：功能增量提取为**去硬编码版**入主仓库；带路径当前版归档 logs/，不采用

**SP `wisdom_ui.html`（863 行 vs CTP 705 行）**：多 124 行，需逐段审增量内容（可能同属普通人对话 UI）

## 七、README 过时描述清单

| 位置 | 过时内容 | 应更正为 |
|---|---|---|
| L8 | 71 个 MCP 工具 | 73 个 MCP 工具 |
| L111 | 71 工具 | 73 工具 |
| L552 | 71 项工具 | 73 项工具 |
| L12 | 3061 节点知识图谱 | 需核实（当前发布库 3048 节点/运行库 56328 节点） |

## 八、待用户拍板的处理决策

1. **B 组三文件**（semantic_translate/chat_engine/code_compose）从 CTP 恢复到 SP —— 立即执行？
2. **C 组 pattern_separation** 补入主仓库（运行必需，MCP 已注册）—— 执行？
3. **D 组普通人对话功能**：提取去硬编码版入主仓库，还是归档历史？
4. **代码工具移除**：code_compose/code_qa 从 server 移除是有意为之还是事故？（影响 dsh-memory README 工具表修正）
5. **智能论历史版本**（1.1/2.9/3.0/3.2）是否移入 docs/history，顶层只留 3.3？
6. **README 数字更正**（71→73 等）随第二阶段一并执行？
