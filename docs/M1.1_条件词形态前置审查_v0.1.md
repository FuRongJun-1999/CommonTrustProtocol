# M1.1 前置审查 · 条件词形态统计（2026-08-29 心跳）

> 行动项 1（条件路由图精确化）的数据底座：写边界规范前先摸清现状形态。

## 统计（wisdom-book-cloud.db 全量 2908 卡）

| 形态 | 数量 | 占比 |
|---|---|---|
| 问X 问句形态（「问插入排序」） | 5653 | **98.6%** |
| 描述句形态（长句） | 56 | ~1% |
| 短关键词（≤6 字无空格） | 25 | <1% |
| 空格分隔多词 | 4 | <0.1% |

## 边界问题实例（规范要消除的歧义形态）

1. **括号同义词混入**：「问GIL（全局解释器锁）」——条件词与注释混杂，
   检索时分词行为不可预期
2. **空格歧义**：「问1 2 3 4 5是什么数」——多词条件词，边界不可判定
3. **长术语重叠**：「问中国特色社会主义理论」vs「问习近平新时代中国特色社会主义思想」
   ——两卡条件词前缀包含，路由归属需要最长匹配规则显式化

## 规范 v1.0 草案要点（下轮产出正式文档）

- R1 形态统一：条件词一律「问X」问句形态（现状 98.6% 已符合，规范固化）
- R2 单义性：一个条件词一个意图——禁止括号内嵌同义词（拆成两个条件词）
- R3 无空格：空格分隔的多词必须合并或拆分为独立条件词
- R4 最长匹配：前缀包含的条件词对，路由归属=最长匹配优先（显式规则）
- R5 同义词归并：显式 synonyms 字段承载变体（不混入主条件词）

## 补充：R4 路由行为抽验（2026-08-29 心跳）

条件词 6484 个、子串包含对 1834 对中抽验 4 组（短词⊂长词形态）：
**4/4 双命中**——card_route 对短/长查询均能归位到语义正确的卡
（短查询「问AI伦理」命中 AI 伦理安全卡属语义覆盖的自然排序）。

**R4 结论修正**：子串包含对不构成路由失败——路由按语义相似度排序
自然处理，无需改条件词；R4 规范条款落点调整为「路由实现确认排序
行为」（已验证）+ 新增条件词时避免与既有条件词完全同名。

## M1.1 阶段小结

- R1-R3：违规 188 → 机械修正 61 → 剩余 R1 语义类 127（分批改写中）
- R4：路由行为验证通过（无需改词）
- R5：同义词归并随 R1 改写同步进行
