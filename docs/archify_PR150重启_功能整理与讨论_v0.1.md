# archify PR #150 重启 · 功能整理与讨论稿 v0.1（2026-09-01）

> 荣指令：认知图项目有新进展，重新参与 archify PR #150；**约束=提交内容不得
> 包括我们自己的图像处理功能**；先整理功能，再讨论。
> PR 现状：OPEN · CHANGES_REQUESTED（两轮评审，tt-a1i 提出契约级阻塞）。

## 一、PR #150 阻塞项（修复 = 重启的前提）

| # | 评审阻塞 | 内容 | 修复方向 |
|---|---|---|---|
| B1 | **路由字段不驱动渲染** | 改 `confidence` 0.88→0.01、`edge.role` main→reject 后 SVG 字节不变——字段是摆设，违反"数据驱动"契约 | 让 schema 字段映射到视觉通道：confidence→节点尺寸/透明度，role(main/reject)→边样式（实线/虚线红），edu_level(E1-E5)→分层着色 |
| B2 | preview runtime 未注册 | `archify preview cognition ...` 报 Unknown diagram type | preview.mjs 注册表补 cognition |
| B3 | card-hit badge 重复 | 同卡命中渲染多个徽章 | 渲染器去重 |

**修复主张**（与协议对齐）：B1 正是我们"条件驱动渲染"的核心论点——**字段不是
注解而是语义本体**，verdict/confidence/role 必须可见。这也让 cognition 类型
区别于普通流程图：展示"为什么这条路被选、那些路被排除"。

## 二、认知图新进展功能盘点（8-31 ~ 9-01）

### A 层：立即可贡献（通用能力，无图像耦合）

| 功能 | 产物 | 贡献形态 |
|---|---|---|
| 数据驱动渲染修复 | B1/B2/B3 修复 | **本 PR 的 fix commit** |
| subgraph 方向查询 | `subgraph(direction=in/out)` | 渲染器支持子图展开方向语义（PR 后续增强） |
| 统一时间核 lint | `time_core_lint.py`（E5 口径） | 独立工具/CI 集成建议（"任何第二种核形状都是 bug"的机械化） |
| 可读性三层扫描 | `readability_l2_scan.py` | 独立工具（ast 定向遍历，闭包不入公开 API 口径） |

### B 层：需泛化后贡献（剥离图像语义，保留结构模式）

| 功能 | 图像耦合点 | 泛化后形态 |
|---|---|---|
| subgraph_replace 子图替换 | 当前 tags/坐标用 image2d | 泛化为「任意模态子树替换」API + 示例改用文本/通用节点——**子部件替换是通用图操作**，图像只是我们的私有应用 |
| 嵌套认知图 schema 模式 | 示例数据是图像语义节点 | 仅贡献「subgraph 递归 + part_of 层级边」的 schema 模式（5 层嵌套验证过），示例数据换成知识域（如 历史学⊃初中历史⊃章节） |
| D 序列落库+趋势回放 | 无硬耦合 | 「gap_trend 观测序列持久化+跨进程回放」模式（对 archify 的验证场景有价值） |

### C 层：明确排除（图像处理 · 荣约束）

- ❌ 子部件提取理论稿 v0.1-v0.4（世界模型图像语义线）
- ❌ 图像大域语义条件卡 v0.1/v0.2
- ❌ 图像识别与分割工程化方案
- ❌ dsh_nested_cg_roundtrip 的图像语义示例数据（18 节点人物分解）
- ❌ vision_store / YOLOVisionProvider 等视觉模块

### D 层：不贡献（内部运维/私有配置）

- WAL 治理三层（watcher/悬挂事务防线）——部署运维性质
- search_providers 可插拔搜索（属 aeis 引擎非认知图）
- 题库/知识卡内容（中文知识资产）

## 三、讨论点（请荣定）

1. **节奏**：先只修 B1-B3 让 PR 过审，还是 fix + B 层增强一并提交？
   （建议：先 fix 过审合入，B 层作为后续独立 PR——小步快跑，降低复审成本）
2. **B 层泛化批次**：subgraph_replace 泛化（无图像版）是否值得作为
   第二个 PR（"子图替换：认知图的可演化性"叙事）？
3. **治理层展示**：统一时间核/负路由/BLINDSPOT 的**设计文档**（非代码）
   是否作为 archify 文档页贡献（提升 cognition 类型的理论背书）？
4. **回归纪律**：archify 侧是否有自己的测试基线需要对齐（B1 渲染改动
   需 golden SVG 重新生成）？

## 四、执行估算（荣定向后）

- B1 数据驱动渲染：1-2 批心跳（schema→渲染映射+golden 重新生成）
- B2/B3：1 批（注册+去重，机械改动）
- B 层泛化（若批）：每项 1-2 批
