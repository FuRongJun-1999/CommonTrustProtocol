# 自举任务书 v0.1（长期任务 · 白箱自写）

> 角色纪律（荣 2026-08-28 指令）：**自举时代码由白箱自己编写**；
> 外部协作者（zcode）仅可提供①规范（本任务书）②对结果的外部校准（跑测试判定）。
> 生成通道：A 白箱确定性（code_compose/navigate/数据生成器自扩展·零 LLM）；
> B LLM 初稿（GLM-5.3-Flash）→ 白箱校验器验证 → 过才固化（需 API key）。

## 锁定基线（自举不得破坏）
六域 1058 case · 编译器 272 · P 线 240 · 图库 123 · 导航 9 · 固化 619 · 工具 77 · KCCS 100%

## 任务队列

### T1 · R2-1 路由缺口白箱自检与补全（通道 A，优先）
- 规范：card_route 对全量 kp 卡的生效条件做命中自检；凡「生效条件字面含
  问题子串却未进 top-k」或「top 命中分 ≤1 且为 bigram 碎片」的卡 → 生成
  触发词补丁（追加到该卡 KCCS 生效条件表）；补丁必须经三段筛选回归验证。
- 已实证缺口：度分布（图监控）、三次握手（网络-TCP握手）。
- 验收：补丁应用后 card_route 对原缺口问题命中目标卡 raw≥2；六域回归不变。

### T2 · R2-2 负路由统一门控计票（通道 A）
- 规范：card_route 负路由从 any()（≥1 排除）升级为计票制——不适用条件
  命中数 ≥2 才排除（对齐实现文档 ccg.search 口径），单命中降权不排除。
- 验收：三角形卡（不适用条件 5 条）对「三角形内角和」仍 ACCEPT；
  「三角形面积」查询被 REJECT。

### T3 · 复合节点扩充（通道 A·数据自举）
- 规范：按 seed_navigation_nodes 模式，从用户对话高频域手工规范+白箱
  生成子叶卡（每复合 2 叶起步）。

### T4 · Mini-Python 增强族（通道 A/B）
- 已完成：for + range/len/sum 内置。
- 待做：+= 复合赋值、print 多参、str 方法（upper/split）。

### T5 · LLM 通道代码生成（通道 B·等 key）
- 规范：Flash 初稿 → verifier 六层 → 测试回归 → 固化；rejected_paths 留痕。

## 外部校准记录
（每次自举批次后由外部协作者登记：跑什么、结果、裁决）
